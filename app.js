// Thennagam Finance LMS — app bootstrap (window.onload)
// (split from index.html; functions are global by design — called from inline onclick handlers)

window.onload=function(){
  // Nothing from localStorage — all data comes from Google Sheet on login
};
